/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['in.bmscdn.com', 'assets-in.bmscdn.com', 'www.cgv.vn', 'res.cloudinary.com'],
    },
}

module.exports = nextConfig
